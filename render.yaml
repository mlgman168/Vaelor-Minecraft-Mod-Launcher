services:
  - type: web
    name: vaelor-chat-relay
    env: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
